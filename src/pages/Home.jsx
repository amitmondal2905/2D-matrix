import React from "react";

const Home = () => (
  <div>
    <h1>Matrix Calculator</h1>
    <p>
      Use the calculator to generate sum and product matrices based on input row
      and column sizes.
    </p>
  </div>
);

export default Home;
